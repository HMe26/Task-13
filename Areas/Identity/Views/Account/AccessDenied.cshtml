@{
    ViewData["Title"] = "Access Denied";
}

<div class="text-center">
    <h4 class="text-danger mb-3">Access Denied</h4>
    <p class="fs-10 text-muted">You don't have permission to view this page.</p>
    <a asp-area="Identity" asp-controller="Account" asp-action="Login" class="btn btn-primary">Back to Login</a>
</div>
